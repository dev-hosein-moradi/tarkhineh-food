import React, { Fragment } from "react";

import {
  GoogleReCaptcha,
  GoogleReCaptchaProvider,
} from "react-google-recaptcha-v3";
import { PageWrapper } from "../hoc";

const Main = () => {
  return <Fragment></Fragment>;
};

export default PageWrapper(Main, "Main", "home");
